{
  "name": "claude-code-buddy",
  "version": "0.1.0",
  "description": "A virtual pet companion for Claude Code terminal",
  "main": "dist/index.js",
  "bin": {
    "buddy": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "postinstall": "node -e \"if(require('fs').existsSync('dist/scripts/setup.js'))require('./dist/scripts/setup.js')\"",
    "prepublishOnly": "npm run build"
  },
  "files": ["dist/"],
  "engines": { "node": ">=18.0.0" },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.0.0"
  },
  "license": "MIT"
}
